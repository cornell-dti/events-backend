{
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    },
    {
      "url": "heroku/nodejs"
    }
  ],
  "description": "Review applications setup for Events Backend on Heroku.",
  "env": {
    "ALLOWED_HOSTS": {
      "required": true
    },
    "AWS_ACCESS_KEY_ID": {
      "required": true
    },
    "AWS_REGION": {
      "required": true
    },
    "AWS_SECRET_ACCESS_KEY": {
      "required": true
    },
    "S3_BUCKET_NAME": {
      "required": true
    },
    "CORS_ORIGIN_ALLOW_ALL": {
      "required": true
    },
    "GOOGLE_BACKEND_CLIENT_ID": {
      "required": true
    },
    "SECRET_KEY": {
      "required": true
    },
    "DEBUG": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "name": "events-backend",
  "scripts": {
  },
  "stack": "heroku-18"
}
